# SmartKeep — Codex Instructions

Read and follow ALL rules in `CONVENTIONS.md` at the project root before making any changes.

Key rules: Clean Architecture layers, 150/200 line split rule, Hook vs Util separation, React.memo + useCallback + useMemo for performance, strict TypeScript, StyleSheet.create() only, theme token colors, @/ path alias imports.
